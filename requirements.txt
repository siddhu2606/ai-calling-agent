# --- Core AI pipeline ---
faster-whisper>=1.0.0      # local speech-to-text (offline, no API key)
edge-tts>=6.1.0             # free neural text-to-speech (no API key)
anthropic>=0.40.0           # Claude LLM brain (default provider)
openai>=1.50.0              # optional: alt LLM provider
elevenlabs>=1.9.0           # optional: cloned/premium voice TTS

# --- Audio I/O (local demo mode) ---
sounddevice>=0.5.0
soundfile>=0.12.1
numpy>=1.26.0
audioop-lts>=0.2.1          # audioop was removed from stdlib in Python 3.13+; needed for Twilio mulaw<->pcm conversion

# --- Config / utils ---
python-dotenv>=1.0.1
pyyaml>=6.0.1

# --- Telephony (real phone calls via Twilio) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0
twilio>=9.3.0
